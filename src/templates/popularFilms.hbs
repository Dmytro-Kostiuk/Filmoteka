{{#each this}}

<li class="film-item">
    <img class="img-films" data-id="{{id}}" src="https://image.tmdb.org/t/p/w500{{poster_path}}" alt="{{title}}" />
    <h2 class="tittle-film">{{title}}</h2>
    <p class="genre-film">{{#each genres}} {{name}} {{/each}} | {{release_date}}</p>
</li>
{{/each}}